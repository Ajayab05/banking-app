version: "3.8"

services:

  backend:
    build: ./backend
    ports:
      - "8081:8080"
    environment:
      DB_HOST: database-1.cgnusuqkc3lr.us-east-1.rds.amazonaws.com
      DB_PORT: 3306
      DB_NAME: banking
      DB_USER: admin
      DB_PASSWORD: admin123

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
